export default function ListStartupBreadcrumb() {
    return (
        <nav className="text-gray-400 mb-8 flex items-center gap-2">
            <span>Dashboard</span>
            <span className="mx-1">{'>'}</span>
            <span className="text-white">List Startup</span>
        </nav>
    );
} 